import UIKit

extension UIImage {
    enum Common {
        static let checkmark: UIImage = UIImage(named: "checkmark")!
    }

    enum Navigation {
        static let closeBarButton: UIImage = UIImage(named: "closeBarButton")!
    }

    enum Tabs {
        static let flightSearchTab: UIImage = UIImage(named: "flightSearchTab")!
        static let preferencesTab: UIImage = UIImage(named: "preferencesTab")!
    }
}
